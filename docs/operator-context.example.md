# Operator Context Example

This file is a safe placeholder for startup context. Replace it with project-specific public or local instructions for the Codex session.

Suggested content:

- Who the assistant is serving in this deployment.
- Which repository or workspace is the main work area.
- Which files are safe to read on startup.
- Which commands are bridge commands and should not enter model context.
- Which local files must never be sent through Telegram.
